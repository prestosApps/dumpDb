create table aircraft(
    hexcode TEXT,
    squawk TEXT,
    flight TEXT,
    lat REAL,
    lon REAL,
    altitude INT,
    vertRate INT,
    track INT,
    speed INT,
    messages INT,
    mlat INT,
    timeSeen INT,
    rssi REAL
);
